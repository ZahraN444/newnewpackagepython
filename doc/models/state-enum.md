
# State Enum

The state of the export file, which can be `in_progress`, `failed` or `succeeded`.

## Enumeration

`StateEnum`

## Fields

| Name |
|  --- |
| `IN_PROGRESS` |
| `FAILED` |
| `SUCCEEDED` |

